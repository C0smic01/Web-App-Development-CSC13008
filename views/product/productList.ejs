<div class="grid grid-cols-3 w-full gap-8  mt-12">
    <% if (products && products.length > 0) { %> 
        <% products.forEach(product => { %>
            <%- include('product', { product }) %>
        <% }); %>
    <% } %>

</div>